# MINISHELL
MiniShell is a simple Linux shell developed in C that can execute built-in and external commands. It supports commands like cd, pwd, echo, and exit, along with external Linux commands using fork(), execvp(), and waitpid(). The project helps in understanding Linux system programming, process management, and shell implementation concepts.
